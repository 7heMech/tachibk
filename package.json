{
  "name": "tachibk",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun tools/build.ts",
    "refresh": "bun tools/refresh-parsers.ts",
    "coverage": "bun tools/coverage.ts",
    "test": "bun tools/test.ts",
    "deploy": "bun tools/test.ts && wrangler deploy"
  },
  "devDependencies": {
    "jsdom": "^29.1.1"
  },
  "engines": {
    "bun": ">=1.2.0"
  }
}
